# Django Settings
SECRET_KEY=django-insecure-changeme-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,web

# Database Settings
DB_NAME=immogab_db
DB_USER=immogab_user
DB_PASSWORD=immogab_password
DB_HOST=db
DB_PORT=5432

# Redis and Celery
REDIS_URL=redis://redis:6379/0

# Optional: DATABASE_URL (takes precedence if set)
# DATABASE_URL=postgres://immogab_user:immogab_password@db:5432/immogab_db
